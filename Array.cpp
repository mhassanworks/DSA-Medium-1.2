#include <iostream>
using namespace std;

int main() {
    // Declare and initialize array
    int numbers[5] = {10, 20, 30, 40, 50};

    // Print all elements
    for (int i = 0; i < 5; i++) {
        cout << "Element at index " << i << " is " << numbers[i] << endl;
    }

    return 0;
}
